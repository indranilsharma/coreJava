/*
 //For XOR operations, 1 XOR 0 produces 1, as does 0 XOR 1. Any other combination produces 0.
//An XOR gives a 1 result if one or the other operand, but not both, is 1. 
 */
package com.operators.bitwiseOperator;

/**
 *
 * @author root
 */
public class Manager6 {
     public static void main(String[] argv) {
    int i = 2;
    int b = 4;
    System.out.println(i ^ 4);//6
  }
}
